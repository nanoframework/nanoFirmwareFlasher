# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The project guide (commands, architecture, CLI verb pipeline, supported platforms, exit codes, conventions, testing, CI, known issues) lives in `.github/copilot-instructions.md`, which is the single source of truth shared with GitHub Copilot. It is imported below. Do not duplicate its content here; when architecture, commands or conventions change, update that file instead.

@.github/copilot-instructions.md
